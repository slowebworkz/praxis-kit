node_modules/
.pnpm-store/
dist/
*.tsbuildinfo
lib/**/*.d.ts
lib/**/*.d.ts.map
lib/**/*.js.map
.DS_Store
.env
.env.local
coverage/
pnpm-debug.log*
**/.playwright/
**/playwright/.cache/

# Local-only Claude guidance — kept out of the repo intentionally.
CLAUDE.md

# .vscode is local-only — editor tasks, migration notes, and lint config are not
# versioned with the repo (also excluded by the global gitignore; kept explicit here).
.vscode/

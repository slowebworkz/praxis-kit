// Moved to @praxis-kit/foundation (see DECISIONS.md); re-exported here so every existing consumer
// of @praxis-kit/primitive's public surface keeps working unchanged.
export type { AnyFunction } from '@praxis-kit/foundation'

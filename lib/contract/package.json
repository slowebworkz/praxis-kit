{
  "name": "@polymorphic-ui/contract",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Contract runtime: ARIA engine, children validator, strict mode",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:related": "vitest run --changed"
  },
  "dependencies": {
    "@polymorphic-ui/primitive": "workspace:*",
    "type-fest": "catalog:"
  },
  "devDependencies": {
    "vitest": "catalog:"
  }
}

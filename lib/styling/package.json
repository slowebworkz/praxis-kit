{
  "name": "@praxis-kit/styling",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "sideEffects": false,
  "description": "Styling runtime: variant resolver, class pipeline, plugin API",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:related": "vitest related --run"
  },
  "dependencies": {
    "@praxis-kit/primitive": "workspace:*",
    "@praxis-kit/primitive": "workspace:*",
    "class-variance-authority": "catalog:",
    "clsx": "catalog:",
    "type-fest": "catalog:"
  },
  "devDependencies": {
    "vitest": "catalog:"
  }
}

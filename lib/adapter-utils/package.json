{
  "name": "@polymorphic-ui/adapter-utils",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:related": "vitest run --related"
  },
  "dependencies": {
    "@polymorphic-ui/core": "workspace:*"
  },
  "devDependencies": {
    "vitest": "catalog:"
  }
}

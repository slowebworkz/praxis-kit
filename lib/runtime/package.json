{
  "name": "@praxis-kit/runtime",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "sideEffects": false,
  "description": "Backend-agnostic render runtime — praxis-kit's own rendering machinery for framework-less environments (web/lit adapters, SSR, the Vite compiler)",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:related": "vitest run --changed"
  },
  "dependencies": {
    "@praxis-kit/pipeline": "workspace:*",
    "@praxis-kit/primitive": "workspace:*",
    "@praxis-kit/styling": "workspace:*",
    "type-fest": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "vitest": "catalog:"
  }
}

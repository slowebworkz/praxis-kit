{
  "name": "@praxis-ui/example-preact",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "eslint . --fix --config eslint.config.ts",
    "lint:check": "eslint . --config eslint.config.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@praxis-ui/core": "workspace:*",
    "@praxis-ui/preact": "workspace:*",
    "@praxis-ui/tailwind": "workspace:*"
  },
  "devDependencies": {
    "@testing-library/preact": "catalog:",
    "jsdom": "catalog:",
    "preact": "catalog:",
    "vitest": "catalog:"
  }
}

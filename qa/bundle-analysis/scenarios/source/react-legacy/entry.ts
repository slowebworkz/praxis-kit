/**
 * Full-entry composition scenario for `@praxis-kit/react/legacy` — the pre-`render`-callback API
 * surface (asChild-only polymorphism). A distinct subpath from `@praxis-kit/react`, so its own
 * scenario, tracked separately from the current API's cost.
 */
export * from '@praxis-kit/react/legacy'

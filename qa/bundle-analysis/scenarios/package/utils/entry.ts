/** Package-tier only — see scenarios/package/contract/entry.ts for why. `praxis-kit/utils` is the
 * smallest possible framework-neutral entry (re-exports only `memoize`). */
export * from 'praxis-kit/utils'

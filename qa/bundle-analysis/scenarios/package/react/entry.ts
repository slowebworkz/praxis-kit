/**
 * Package-boundary counterpart to scenarios/source/react: imports `praxis-kit/react` (resolved via
 * ordinary node module resolution against packages/kit's built dist/, not aliased to source). See
 * scripts/analyze.ts for the source-vs-package tier rationale.
 */
export * from 'praxis-kit/react'

/** Package-tier only — see scenarios/package/contract/entry.ts for why. `praxis-kit/guards`
 * re-exports only from @praxis-kit/primitive — no core, no diagnostics, no adapter. */
export * from 'praxis-kit/guards'

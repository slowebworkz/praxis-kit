# @praxis-kit/metrics

Metrics collection, snapshotting, and reporting for the monorepo — the numeric memory of the QA
layer. Aggregates measurements from the other `qa/` workspaces (bench results, bundle sizes) into
snapshots so trends are visible across commits rather than only point-in-time.

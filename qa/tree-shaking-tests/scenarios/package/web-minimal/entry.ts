/**
 * Package-boundary scenario for `praxis-kit/web` — no framework peer at all (vanilla custom
 * elements); see react-minimal/entry.ts for the package-vs-source rationale.
 */
import { createContractComponent } from 'praxis-kit/web'

export { createContractComponent }

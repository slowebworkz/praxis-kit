/**
 * Package-boundary counterpart to scenarios/source/vue-minimal — see react-minimal/entry.ts
 * for the rationale.
 */
import { createContractComponent } from 'praxis-kit/vue'

export { createContractComponent }

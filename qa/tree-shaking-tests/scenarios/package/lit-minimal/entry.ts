/**
 * Package-boundary scenario for `praxis-kit/lit` — no source-scenario counterpart exists yet
 * (lit/web adapters landed after the original scenario set); see react-minimal/entry.ts for the
 * package-vs-source rationale.
 */
import { createContractComponent } from 'praxis-kit/lit'

export { createContractComponent }

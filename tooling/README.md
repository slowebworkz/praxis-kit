# tooling/

Developer-facing command-line tools.

| Workspace  | Package               | Published entry      | Purpose                                     |
| ---------- | --------------------- | -------------------- | ------------------------------------------- |
| `codemod/` | `@praxis-kit/codemod` | `praxis-kit/codemod` | Codemods for migrating between kit versions |

The codemod CLI ships inside the published `praxis-kit` package and is exposed as the
`praxis-codemod` bin. See [tooling/codemod/README.md](codemod/README.md) for available transforms.

// The eight framework-neutral state-prop normalizers (`disabled` → `aria-disabled`/`data-*`, etc.).
// They live in `@praxis-kit/contract/props`; this is the `@praxis-kit/core` entry point for them.
export * from '@praxis-kit/contract/props'

{
  "extends": "../../tsconfig.base.json",
  // map private adapter-utils to source so tsup's dts bundler inlines it (tsconfig.build.json extends this)
  "compilerOptions": {
    "paths": {
      "@praxis-ui/adapter-utils": ["../../lib/adapter-utils/src/index.ts"]
    }
  },
  "include": [
    "src",
    "./vitest.config.ts",
    "./vitest.ssr.config.ts",
    "./tsup.config.ts"
  ]
}

// Public surface for general-purpose utilities praxis-kit uses internally.
// Consumers get the same helpers praxis-kit already has in-house, rather than
// hand-rolling them.
export { memoize } from '@praxis-kit/primitive'
